<?php
/*
 * Точка розширення Flarum: тут програмно вмикають, вимикають і донастроюють
 * розширення. Файл прокинутий із хоста, тому переживає перезбірку образу
 * і лежить у git.
 */

use Flarum\Extend;

return [
    /*
     * Оформлення форуму під тему сайту f1monkey.
     *
     * Через extend.php, а не через «Custom LESS» в адмінці: те, що лежить
     * у БД, зникає після `docker compose down -v` і не видно в діффі.
     *
     * Файл додається останнім у ланцюжку стилів форуму, тому його :root
     * перекриває змінні ядра без !important. Після зміни less/ потрібно
     * `php flarum assets:publish` — інакше браузер отримає стару збірку.
     */
    (new Extend\Frontend('forum'))
        ->css(__DIR__ . '/less/f1monkey.less'),
];
